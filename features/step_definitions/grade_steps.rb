$LOAD_PATH.unshift File.expand_path('../../../lib', __FILE__)
require 'gdata'
include Gradebook

Given /^a column id after searching for a category name$/ do
  
end

When /^I issue the grade command$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^reveal the students names sequentially each time asking for which grade should be inserted$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^insert the grade in the studnets row and the cateogry column$/ do
  pending # express the regexp above with the code you wish you had
end
